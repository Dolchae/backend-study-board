spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root  # MySQL ?? (??? ??)
    password: qufrjdkslsekdeod97@!  # MySQL ???? (??? "" ? ???)
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ?? ??? update, ????? validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true



